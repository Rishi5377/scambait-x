# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# LLM
langchain-groq>=0.1.0

# HTTP Client (for GUVI callback)
httpx>=0.26.0

# Utils
aiohttp>=3.9.0

# Note: Redis, PostgreSQL, Celery removed for Render free tier
# The app works with in-memory fallback
